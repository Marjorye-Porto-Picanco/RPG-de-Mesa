@* Layout "vazio": usado pelas telas do RPG de Mesa (Login, Cadastro, Campanhas,
   Mesa, Personagens, Regras) para que elas ocupem a tela inteira, sem herdar
   a barra lateral/top-row do MainLayout padrão do template MAUI. *@
@inherits LayoutComponentBase

@Body
